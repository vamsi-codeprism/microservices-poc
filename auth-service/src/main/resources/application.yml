server:
  port: 9001

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate # Avoid conflict with Flyway
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/auth-service

  security:
    auto-configure:
      enabled: false

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      auto-offset-reset: earliest
      group-id: auth
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      properties:
        spring.json.trusted.packages: "*"
    topic:
      name: order-topic

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8767/eureka/

jwt:
  secret: 3dWxzzJpaD5Wd9Duf59Fl22sdDA29EcwMDmID0Yfq/ixNEgBozKHFae2b9IBrb4BCv6XrKFcQdfaIzLnmdTeDg==
  expiration: 86400000

